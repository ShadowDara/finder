server {
    listen 3016;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Reine statische Site — kein /api/-Proxy, kein Backend
    location / {
        try_files $uri $uri/ /index.html;
    }
}
