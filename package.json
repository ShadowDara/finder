{
  "name": "finder",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "stats": "pnpm linguist-js",
    "gotest": "go test -coverprofile=coverage ./... go tool cover -html coverage.out"
  },
  "version": "0.3.18",
  "devDependencies": {
    "@shadowdara/linguist-js": "*",
    "@shadowdara/seg": "0.2.4",
    "automatic-md-index": "^1.3.7",
    "cross-env": "^10.1.0",
    "finder-npm": "^0.0.0",
    "http-server": "^14.1.1"
  }
}
